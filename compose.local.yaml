services:
  frontend:
    ports:
      - "127.0.0.1:${FRONTEND_PORT:-3000}:3000"
  backend:
    environment:
      INFRARENDER_ENV: development
    ports:
      - "127.0.0.1:${BACKEND_PORT:-8000}:8000"